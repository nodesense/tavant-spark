package examples

object ProductExample extends  App {
  // define a class called Product,
  // it has member variables, no (string), price (double) category (string, "mobile", "wahine")

  // setter function, that assign value to no, price, category  setCategoy
  // getter function, that returns no, price, category, getCategory

  // toString, that prints Product(1234, 99.99, "mobile")

}
